// Explicación: Este ejercicio tiene un botón que debería alternar la visibilidad del texto.
// Completa la función toggleText() para que muestre/oculte el texto según sea necesario.

import { useState } from 'react';

export default function ToggleText() {
//   Variable

  // Completa esta función para mostrar u ocultar el texto
  function toggleText() {
    alert('Aquí va tu código');
    // Tu código debería alternar el estado de isVisible
  }

  return (
    <div>
      <button onClick={toggleText}>
        
      </button>
      
    </div>
  );
}
